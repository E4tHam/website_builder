#include <iostream>
#include <fstream>
#include <string>

using namespace std;

#include "website.h"

int main ( int argc, char * argv[] ) {
    // find all flags

    // find all dependencies
    // if contains index build index
    // if Dependencies.size()==0, build all

    Website().build();
    cout << "done\n";

}
